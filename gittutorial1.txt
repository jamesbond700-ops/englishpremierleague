git config --global user.name "jamesbond700-ops"
git config --global user.email "nirdeshbahadurkoirala@gmail.com"
git config --global core.editor "/usr/bin/vim"
git config --list
cat ~/.gitconfig

to delete git rm -f filename

git status -s view the output in shortened format
git status -v get more verbose output, including what was changed in a file
git status -a -m " it will add and commit the file once it is modified
